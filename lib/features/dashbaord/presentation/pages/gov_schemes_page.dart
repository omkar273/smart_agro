import 'package:flutter/material.dart';

class GovSchemesPage extends StatelessWidget {
  const GovSchemesPage({super.key});
  static const routeName = 'GovSchemesPage';
  static const routePath = '/GovSchemesPage';
  @override
  Widget build(BuildContext context) {
    return SafeArea(
      child: SingleChildScrollView(
        child: Column(
          children: [],
        ),
      ),
    );
  }
}
